import { ViewHeader } from "vanilla-components";

export const Full = () => (
  <ViewHeader eyebrow="Stages" title="Capture" sub="live transcription from active taps" />
);

export const Plain = () => <ViewHeader title="Overview" />;
