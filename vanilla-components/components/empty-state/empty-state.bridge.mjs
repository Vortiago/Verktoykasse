// @ts-check
// design-sync bridge contract for empty-state (read by bridge/emit-adapter.mjs):
// the agent-facing Props body (narrowed). shim defaults to "declarative".
export default {
  props: `icon?: string;\n  title: string;\n  detail?: string;`,
};
