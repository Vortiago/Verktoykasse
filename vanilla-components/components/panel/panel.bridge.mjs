// @ts-check
// design-sync bridge contract for panel (read by bridge/emit-adapter.mjs):
// the agent-facing Props body (narrowed). shim defaults to "declarative".
export default {
  props: `head?: string;\n  body?: string;\n  fill?: boolean;`,
};
